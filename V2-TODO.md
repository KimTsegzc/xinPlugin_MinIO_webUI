# xinPlugin_MinIO_webUI — V2 待办（完成状态）

> 版本目标：V2.x  完成日期：2026-08-24
> 已按功能点拆成 v2.0–v2.9 逐个小版本发布（各含 commit + tag）。✅ = 已发布，⏸ = 待外部插件/后续（见文末说明）。

## 一、格式多样化 / 适配 Office 等

- [x] ✅ 预览支持 `.yaml / .yml / .xml / .log` 等文本类（v2.1，作为文本预览）。
- [x] ✅ `.json` 格式化缩进展示（v2.1）。
- [x] ✅ 代码类文件（`.js/.ts/.py/.go/...`）文本预览（v2.1，纯文本；CodeMirror 见文末）。
- [x] ✅ `.csv` 表格化展示（v2.1，带表头/基础表格）。
- [x] ✅ 未知/二进制类型降级：可下载 + 占位提示，不报错（v2.1）。
- [ ] ⏸ 预览支持 `.docx / .xlsx / .pptx`（Office 三件套）——依赖 better-sidebar 的 Office 预览插件接入，待确认其 API。

## 二、文件类型 / Bucket 图标优化

- [x] ✅ 按扩展名区分图标（目录/图片/PDF/Word/Excel/PPT/代码/文本/压缩/音视频/未知）（v2.0）。
- [ ] ⏸ Bucket 图标区分本地/远端、状态角标——当前单端点场景价值低，暂缓。
- [x] ✅ 图标用主题色一致的固定配色 SVG（v2.0）。

## 三、交互 / 体验

- [ ] ⏸ 文件菜单「引用」接入 DSH 附件管道——需宿主把文件拉取后走 DSH 附件/引用内部 API，待确认其契约。
- [x] ✅ 多选 / 批量删除（v2.5，仅文件、二次确认）。
- [x] ✅ 拖拽上传进度条（文件级）+ 上传取消（v2.6，AbortController）。
- [x] ✅ 搜索/过滤（文件名/类型）（v2.4）。
- [x] ✅ 文件详情（预览弹窗详情条：类型/大小/修改时间）（v2.3）。
- [x] ✅ 排序（名称/大小/时间、升降序、文件夹置顶）（v2.2）。
- [x] ✅ 图标/列表视图记忆用户偏好（localStorage）（v2.3）。

## 四、宿主 / 稳定性

- [ ] ⏸ 宿主热更新问题——挂载包 host 半需进程重启才生效，属 DSH 机制，README 已文档化「改 host 需重启」。
- [ ] ⏸ 上传大文件真·流式——当前 base64+JSON 可用，流式需新端点+客户端 XHR，高风险未做。
- [x] ✅ 密钥不再经 `--user` 传参进 curl——进程内自实现 AWS SigV4 签名（v2.8）。
- [x] ✅ 临时文件 `.minio-*.bin/.b64` 清理机制（v2.7，用后即删）。
- [x] ✅ 连接超时/失败重试、友好错误提示（v2.7，`--connect-timeout/--max-time/--retry` + 中文错误映射）。

## 五、配置

- [x] ✅ SSL 开关细化（http/https，自签名证书 `-k`）（v2.9）。
- [ ] ⏸ 多桶差异化配置（每桶覆盖 endpoint/ak/sk）——需客户端 UI + 宿主按桶解析配置，暂缓。
- [x] ✅ 配置加密存储（密钥不明文）——secretKey AES-256-GCM 落盘（v2.9）。

## ⏸ 待外部插件 / 后续说明

1. **Office 预览**（.docx/.xlsx/.pptx）需先确认 better-sidebar 的 Office 预览插件及接入 API。
2. **代码 CodeMirror 预览**需确认 better-sidebar 编辑器插件的挂载方式（当前已按纯文本预览）。
3. **引用接入 DSH**需 DSH 附件/引用管道的宿主侧契约。
4. **真·流式上传**需新宿主端点 + 客户端 XHR，且宿主改动需重启、需人工可视化回归，风险较高。
5. **多桶差异化配置**需客户端编辑 UI + 宿主按桶合并配置，单端点场景暂缓。

以上均不影响已发布 v2.0–v2.9 的功能。
