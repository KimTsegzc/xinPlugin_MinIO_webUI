# xinPlugin_MinIO_webUI — V2 待办

> 版本目标：V2.0  日期：2026-08-23（草稿）
> 基于 V1.1（上传/下载/删除/拖拽已可用，但宿主 upload 需进程重启生效）持续迭代。

## 一、格式多样化 / 适配 Office 等

- [ ] 预览支持 `.docx / .xlsx / .pptx`（Office 三件套）——由 better-sidebar 的 Office 预览插件承载（设置页「添加插件」→ Office 预览插件），本插件把 Office 文件接入该 viewer。
- [ ] 预览支持 `.yaml / .yml / .xml / .log` 等文本类，语法着色；`.json` 做树形/格式化。
- [ ] 代码类文件（`.js/.ts/.py/.go/...`）交给 better-sidebar 的 CodeMirror 编辑器预览。
- [ ] `.csv` 表格化展示（带表头/列宽/排序，至少基础表格）。
- [ ] 音频/视频 `.mp3/.mp4/.webm` 播放预览（mediaUrl）。
- [ ] 未知/二进制类型降级：可下载 + 占位提示，不报错。

## 二、文件类型 / Bucket 图标优化

- [ ] 按扩展名区分图标：目录、图片(png/jpg/gif/webp/svg)、PDF、Word、Excel、PPT、代码、文本、压缩包、未知 —— 替换当前单一的「文档」图标。
- [ ] Bucket 图标：区分「本地绑定桶 / 远端桶」、「default/自定义」样式；可加 bucket 状态角标（已挂载/异常）。
- [ ] 图标统一走 better-sidebar 的 primitives 图标或主题色 SVG，保证明暗主题一致。

## 三、交互 / 体验

- [ ] 文件菜单「引用」接入 DSH：把 MinIO 文件作为引用（附件/上下文）插入对话（需宿主侧把文件拉取后走 DSH 附件管道）。
- [ ] 多选 / 批量操作（批量下载、批量删除）。
- [ ] 拖拽上传进度条（单文件/总进度）、上传取消。
- [ ] 搜索/过滤（文件名、类型、大小）。
- [ ] 右侧信息栏（文件详情：ETag、大小、修改时间、预览）。
- [ ] 排序（按名称/大小/修改时间，升/降序 + 文件夹置顶）。
- [ ] 图标/列表视图记忆用户偏好。

## 四、宿主 / 稳定性

- [ ] 修复宿主「新增方法需进程重启才生效」的热更新问题（让 host lib 变更能被 HMR 捕获，或文档化重启步骤）。
- [ ] 上传大文件真·流式（当前 base64+JSON，大文件受限）；考虑直传分片或宿主流式转发。
- [ ] 密钥不再经 `--user` 传参进 curl（改自实现 SigV4 签名 + 环境变量传密钥）。
- [ ] 临时文件 `.minio-*.bin/.b64` 清理机制。
- [ ] 连接超时/失败重试、友好错误提示。

## 五、配置

- [ ] SSL 开关细化（http/https，自签名证书）。
- [ ] 多个桶的差异化配置（每桶可覆盖 endpoint/ak/sk）。
- [ ] 配置加密存储（密钥不明文）。
